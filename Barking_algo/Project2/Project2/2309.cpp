#include<iostream>

using namespace std;

int main() {
	std::ios::sync_with_stdio(0);
	std::cin.tie(0);

	int *a = new int[9];
	int* rs = new int[7];
	for (int i = 0; i < 9; i++) {
		cin >> a[i];
	}

}